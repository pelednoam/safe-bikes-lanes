.venv/
__pycache__/
.mypy_cache/
.ruff_cache/
.pytest_cache/
pipeline/cache/
web/node_modules/
test-results/
data/*
!data/overrides.geojson

# coverage output
.coverage
coverage/
htmlcov/

# vendored at build time from node_modules (npm run vendor), so the site
# serves its own copy instead of executing a CDN's script at runtime
web/maplibre-gl.js
web/maplibre-gl.css
