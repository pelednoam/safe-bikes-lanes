{
  "name": "family-bike-router-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit -p tsconfig.check.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "maplibre-gl": "^4.7.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10"
  }
}
